# Flutter-project-Build.Fix
Fix problems with Flutter build. # Permission Fix: make a folder with same name as project, and user permission! ```project=learning ; sudo mkdir $project ; sudo chown -R $USER:$USER $project```
